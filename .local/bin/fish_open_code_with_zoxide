#!/usr/bin/env fish

set folder (cat /dev/null | tofi --prompt-text "Open : ")

if test -n "$folder"

    /usr/bin/code --password-store="gnome-libsecret" (zoxide query (string split -n " " $folder))

end
