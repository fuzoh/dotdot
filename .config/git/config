[core]
	pager = delta

[interactive]
    diffFilter = delta --color-only --features=interactive
    editor = hx

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false

[merge]
    conflictStyle = zdiff3

[diff]
    colorMoved = default
	
[user]
    name = fuzoh
    email = info@bastiennicoud.ch

[commit]
    gpgsign = true

[init]
    defaultBranch = main

[core]
    autocrlf = input

[push]
    autoSetupRemote = true
